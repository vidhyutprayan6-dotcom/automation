# Copy to .env and fill in (never commit .env)
TELEGRAM_BOT_TOKEN=8907395033:AAGzsyt4XUg9xr0bamQGI6426HAb6-H5sNI

# Optional. Primary display is detected automatically.
# If detection fails, run `ffmpeg -f avfoundation -list_devices true -i ""`
# and set the numeric index shown beside "Capture screen 0".
# SCREEN_RECORD_INPUT=1

# Optional. Seconds per recording segment. Testing default is 60 (1 minute).
# Set to 1800 for production 30-minute segments.
# SCREEN_RECORD_SECONDS=60
